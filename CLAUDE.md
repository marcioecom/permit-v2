# Project Rules

## Icons
Always use `@tabler/icons-react` for icons in the dashboard app. Never use inline SVGs for icons.

## Design Language
Maintain consistency with the slate-900/accent design system. Primary button hover should use `hover:bg-slate-800`, not accent color.
